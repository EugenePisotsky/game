name: neura_world
description: Shared world model and simulation for Neura.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.14.0-10.0.dev

resolution: workspace

dependencies:
  vector_math: ^2.2.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
