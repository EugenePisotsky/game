name: neura_world
description: Shared world model and simulation for Neura.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.14.0-10.0.dev

resolution: workspace

dependencies:
  ffi: ^2.2.0
  flutter_rust_bridge: 2.11.1
  hooks: ^1.0.1
  native_toolchain_rust: ^1.0.3
  path: ^1.9.1
  vector_math: ^2.2.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
